apiVersion: v1
kind: Namespace
metadata:
  name: dev
  labels:
    environment: dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: answers
  namespace: dev
  labels:
    app: answers
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: answers
      environment: dev
  template:
    metadata:
      labels:
        app: answers
        environment: dev
    spec:
      imagePullSecrets:
        - name: docker-hub-pull-secret
      containers:
        - name: answers
          image: docker.io/kotenkass/answers
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: answers-dev-env
                  key: DATABASE_URL
            - name: KAFKA_BOOTSTRAP
              valueFrom:
                secretKeyRef:
                  name: answers-dev-env
                  key: KAFKA_BOOTSTRAP
            - name: LOG_LEVEL
              value: debug
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: answers
  namespace: dev
  labels:
    app: answers
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: answers
    environment: dev
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
